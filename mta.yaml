_schema-version: 3.3.0
ID: capgrafana
description: A simple CAP project.
version: 1.0.0-SNAPSHOT
modules:
- name: capgrafana-srv
  type: java
  path: srv
  properties:
    JBP_CONFIG_COMPONENTS: 'jres: [''com.sap.xs.java.buildpack.jre.SAPMachineJRE'']'
    JBP_CONFIG_SAP_MACHINE_JRE: '{ version: 21.+ }'
    SPRING_PROFILES_ACTIVE: cloud,sandbox
  requires:
  - name: capgrafana-auth
  - name: capgrafana-db
  - name: capgrafana-destination
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: sap_java_buildpack_jakarta
    instances: 1
  build-parameters:
    build-result: target/*-exec.jar
    builder: custom
    commands:
    - mvn clean package -DskipTests=true --batch-mode
- name: capgrafana-db-deployer
  type: hdb
  path: db
  requires:
  - name: capgrafana-db
  parameters:
    buildpack: nodejs_buildpack
  build-parameters:
    builder: custom
    commands:
    - npm run build
- name: capgrafana
  type: approuter.nodejs
  path: app/router
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: capgrafana-auth
  - name: capgrafana-html5-runtime
  - name: capgrafana-destination
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  provides:
  - name: app-api
    properties:
      app-protocol: ${protocol}
      app-uri: ${default-uri}
      url: ${default-url}
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: capgrafana-app-deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: capgrafana-auth
  - name: srv-api
  - name: capgrafana-html5-repo-host
    parameters:
      config:
        HTML5Runtime_enabled: true
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - capgrafana_authors.zip
      name: capgrafanacapgrafana_authors
      target-path: resources
    - artifacts:
      - capgrafana_books.zip
      name: capgrafanacapgrafana_books
      target-path: resources
- name: capgrafanacapgrafana_authors
  type: html5
  path: app/capgrafana_authors
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: capgrafanacapgrafana_books
  type: html5
  path: app/capgrafana_books
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: capgrafana-destinations
  type: com.sap.application.content
  requires:
  - name: capgrafana-auth
    parameters:
      service-key:
        name: capgrafana-auth-key
  - name: capgrafana-html5-repo-host
    parameters:
      service-key:
        name: capgrafana-html5-repo-host-key
  - name: srv-api
  - name: capgrafana-destination
    parameters:
      content-target: true
  parameters:
    content:
      instance:
        destinations:
        - Name: capgrafana-html5-repository
          ServiceInstanceName: capgrafana-html5-repo-host
          ServiceKeyName: capgrafana-html5-repo-host-key
          sap.cloud.service: capgrafana.service
        - Authentication: OAuth2UserTokenExchange
          Name: capgrafana-auth
          ServiceInstanceName: capgrafana-auth
          ServiceKeyName: capgrafana-auth-key
          sap.cloud.service: capgrafana.service
        existing_destinations_policy: update
  build-parameters:
    no-source: true
resources:
- name: capgrafana-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
        redirect-uris:
        - https://*~{app-api/app-uri}/**
      tenant-mode: dedicated
      xsappname: capgrafana-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
  requires:
  - name: app-api
- name: capgrafana-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: capgrafana-destination
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
    service: destination
    service-plan: lite
  requires:
  - name: srv-api
- name: capgrafana-html5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: capgrafana-html5-runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
